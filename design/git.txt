git clone https://github.com/vollov/django-poll
git remote add upstream https://github.com/vollov/django-poll
git checkout -b step_01 upstream/master
git commit
git push origin step_01

When upstream (django/django) has changed, you should rebase your work. To do this, use:

git fetch upstream
git rebase